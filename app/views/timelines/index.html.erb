<h1>Timelines#index</h1>

<% if current_user %>
	<a href="<%= new_timeline_path %>">
		<%= button_tag "new timeline" %>
	</a>
<% end %>

<ul>
	<% @timeline.each do |tl| %>
	<li>
		<%= link_to "TIMELINE "+ tl.subject, tl %>
		<%= tl.description %>, <%= tl.created_at.to_s %>
	</li>
	<% end %>
</ul>